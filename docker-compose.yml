version: '3'
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    tty: true
    volumes:
      - .:/home/ubuntu/app
  selenium-chrome:
    image: "selenium/standalone-chrome"
